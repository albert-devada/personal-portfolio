services:
  web:
    image: node:22-alpine
    container_name: personal-portfolio
    working_dir: /personal-portfolio
    ports:
      - "3000:3000"
    volumes:
      - .:/personal-portfolio
    environment:
      - NODE_ENV=development
    command: sh -c "npm install && npm run dev"